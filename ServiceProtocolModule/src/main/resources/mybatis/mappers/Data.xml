<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Data">

    <insert id="add" parameterType="team.hnuwt.protocol.model.Data">
        insert into tb_data(collectorId,meterId,data,valveState)
        values(#{collectorId},#{meterId},#{data},#{valveState})
    </insert>
    
    <insert id="addBatch">
        insert into tb_data(collectorId,meterId,data,valveState)
        values
        <foreach collection="list" item="data" separator=",">
            (#{data.collectorId},#{data.meterId},#{data.data},#{data.valveState})
        </foreach>
    </insert>
    
</mapper>