<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Check">

    <select id="offline" parameterType="int" resultType="String">
        select collectorId from tb_HeartBeat
        where
            current_timestamp - tb_HeartBeat.time &lt; #{checkTime}
        and
            tb_HeartBeat.collectorId = (
                select collectorId from tb_Login
                where tb_Login.state = 1
            )
    </select>



</mapper>